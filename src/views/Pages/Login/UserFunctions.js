import axios from 'axios'

export const register = newUser => {
    return axios
    .post('user/register'), {

    }
}

export const login = user => {
    return axios
    .post('', {
        
    })
}